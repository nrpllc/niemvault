# Stamped by the build. Read at runtime so content compatibility (spec section 7) can be checked
# against what is actually running -- from a jar, from a class directory, and from a test, which a
# jar manifest alone does not cover.
platform.version=${platformVersion}
